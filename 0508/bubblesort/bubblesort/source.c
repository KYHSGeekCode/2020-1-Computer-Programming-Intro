#include<stdio.h>void line_up(int ary[], int count) {	int i, j, k = 0;	int temp;	for (i = 0; i < count; i++)	{		for (j = 0; j < count - i - 1; j++)		{			if (ary[j] > ary[j + 1]) {				temp = ary[j];				ary[j] = ary[j + 1];				ary[j + 1] = temp;			}			printf("반복횟수 : %d\n", k++);		}	}}int main() {	int ary[5];	printf("5개의 정수 입력 : ");	for (int i = 0; i < 5; i++) {		scanf_s("%d", ary + i);	}	line_up(ary, 5);	for (int i = 0; i < 5; i++) {		printf("%d ", ary[i]);	}	printf("\n");}
